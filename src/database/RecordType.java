package database;

/**
 * Class that represents the type of a record in the database.
 */
public enum RecordType {
    BUNDLE, // Represents a Bundle record type
    ITEM, // Represents an Item record type
    CATEGORY // Represents a Catergory record type
}
